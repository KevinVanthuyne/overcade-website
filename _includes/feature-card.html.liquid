<div class="rounded-md border border-neutral-200 bg-white p-8 text-center shadow">
    <div class="button-text mx-auto flex h-16 w-16 items-center justify-center text-indigo-700"
    >

        {% include {{ include.icon }} %}
    </div>
    <h3 class="mt-6 text-gray-900 text-xl font-semibold">{{ include.title }}</h3>
    <div class="my-4 mb-0 font-normal leading-relaxed tracking-wide text-gray-500">{{ include.content }}</div>
</div>