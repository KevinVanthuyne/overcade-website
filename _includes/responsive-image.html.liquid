{% comment %}
Render your responsive images using lazysizes.js

Usage:

	{% responsive_image path: assets/image.jpg alt: "A description of the image" %}

{% endcomment %}

{% assign largest = resized | sort: 'width' | last %}
{% capture srcset %}
    {% for i in resized %}
        /{{ i.path }} {{ i.width }}w,
    {% endfor %}
{% endcapture %}

<!-- responsive example with automatic sizes calculation: -->
<img
        alt="{{ alt }}"
        data-sizes="auto"
        data-src="/{{ largest.path }}"
        data-srcset="{{ srcset | strip_newlines }} /{{ path }} {{ original.width }}w"
        class="lazyload {{ class }}"
/>
